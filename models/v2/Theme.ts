export type Theme = {
  primary: string,
  primaryDisabled: string,
  background: string,
  card: string,
  text: string,
  textDisabled: string,
  border: string,
  muted: string, // Secondary buttons or messages
  shade: string, // Lighter / darker shade color on card
  input: string,
  inputPlaceholder: string,
  red: string, // For errors or destructive actions
}